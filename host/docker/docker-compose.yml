version: "2.1"
services:
    rpisec-postgres:
        container_name: rpisec-postgres
        image: postgres:9.5.6
        environment:
            - POSTGRES_USER=${RPISEC_DB_USER}
            - POSTGRES_PASSWORD=${RPISEC_DB_PASSWORD}
            - POSTGRES_ROOT_PASSWORD=${RPISEC_DB_ROOT_PASSWORD}
            - POSTGRES_DATABASE=${RPISEC_DB_NAME}
        mem_limit: 256m
        cpu_shares: 2
        depends_on:
            - rpisec-nginx
        volumes:
            - ${POSTGRES_DATA_VOLUME}:/var/lib/postgresql/data
    rpisec-nginx:
        container_name: rpisec-nginx
        image: lispmeister/rpi-nginx:0.0.1
        volumes:
            - ${NGINX_CONF_VOLUME}:/etc/nginx
            - ${NGINX_LOG_VOLUME}:/var/log/nginx
            - ${NGINX_CERT_VOLUME}:/tmp/cert
        mem_limit: 128m
        cpu_shares: 2
